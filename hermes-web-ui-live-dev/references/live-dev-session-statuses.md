# Live-dev session statuses

Observed via `mcp_agentmemory_memory_sessions` in this workspace:

- `active`
- `completed`

When asked about session status inventory, query the live session list rather than inferring from older notes.
