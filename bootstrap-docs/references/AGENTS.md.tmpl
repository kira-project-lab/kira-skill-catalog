# <PROJECT NAME> — operating map

<!-- The 60-second orientation for any agent or new engineer. Keep under one page. -->

## What this is

<One paragraph: what the product/service does and for whom.>

## Layout

| Path | What lives there |
| --- | --- |
| `<src dir>` | <what> |
| `docs/adr/` | Architecture Decision Records — durable decisions, indexed in its README |
| `docs/` | <other docs: product, runbooks, audits> |

## Commands

| Task | Command |
| --- | --- |
| Run dev | `<command>` |
| Tests | `<command>` |
| Lint/format | `<command>` |

## Conventions

- Branching: <flow, e.g. feature branches off `<default>`, merge via PR>
- Commits: Conventional Commits (`feat:`, `fix:`, `docs:`, ...)
- Decisions: registered per the documentation contract in `CLAUDE.md`

## Do not

- <hard boundaries: prod access, secrets, generated files, ...>
